# Rename this file to .env and add your environment variables.
# Vite exposes environment variables to the client-side code if they are prefixed with VITE_.
# Example:
# VITE_API_BASE_URL=https://api.example.com